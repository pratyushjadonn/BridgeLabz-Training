spring:
  application:
    name: ContactsApp
  profiles:
    active: dev   # switches to 'prod' when deploying
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
